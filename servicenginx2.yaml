apiVersion: v1
kind: Service
metadata:
  name: nginx2-service # ЭТО ИМЯ БУДЕТ ИСПОЛЬЗОВАТЬСЯ ДЛЯ ДОСТУПА
spec:
  selector:
    app: nginx2 # Ссылка на метку из deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80